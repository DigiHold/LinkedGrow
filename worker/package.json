{
  "name": "linkedgrow-worker",
  "private": true,
  "type": "module",
  "description": "The LinkedGrow worker plane: one persistent browser per LinkedIn account, driven through that account's dedicated address.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types 'src/**/*.test.ts' '../shared/**/*.test.ts'",
    "worker": "node --experimental-strip-types src/worker.ts"
  },
  "dependencies": {
    "@libsql/client": "^0.15.15",
    "dotenv": "^17.2.3",
    "patchright": "1.61.1",
    "undici": "^7.29.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=24"
  }
}
