# GSC weekly reports + OAuth artifacts (never commit)
scripts/gsc/reports/
scripts/gsc/token.json

# Dependencies
node_modules/
/.pnp
.pnp.js
.yarn/install-state.gz

# Testing
/coverage

# Next.js build
/.next/
/out/
/build

# Environment files (NEVER commit these!)
.env
.env.*
.env.local
.env.development
.env.production
.env.*.local
!.env.example

# Database files
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Keys and certificates (NEVER commit these!)
*.pem
*.key
*.crt
*.p12
*.pfx
credentials.json
service-account.json

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE
.idea/
.vscode/

# Claude Code config (may contain credential references)
.claude/
*.swp
*.swo
*.sublime-workspace
*.sublime-project

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
*.log

# Vercel
.vercel

# Cloudflare
.wrangler/
.worker-next/
.dev.vars
.open-next/

# TypeScript cache
*.tsbuildinfo
next-env.d.ts

# Blog image generation scripts (contain API keys)
scripts/generate-blog-images.js

# Generated redemption code CSVs (contain valuable codes - send manually to marketplaces)
redemption-codes-out/

# Misc
*.bak
*.tmp
*.temp
.cache/

# Test snapshots (optional - remove if you want to commit these)
# __snapshots__/

# Local migrations (if using local dev db)
/migrations/


# Package manager locks (keep package-lock.json, ignore others if not using)
# yarn.lock
# pnpm-lock.yaml
